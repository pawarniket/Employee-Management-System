import { DepartmentFilterPipePipe } from './department-filter-pipe.pipe';

describe('DepartmentFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DepartmentFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
